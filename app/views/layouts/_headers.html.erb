<header class=" nav navbar navbar-inverse">
  <a class="navbar-brand center-block" href="#">ChowSource</a>
    <nav class="navbar-right">
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group" id="search-form">
          <%= render 'searches/search' %>
        </div>  
      </form>
      <% if logged_in? %>
          <%= link_to current_user.username, user_path(id: session[:user_id]) %> |
          <%= link_to 'logout', logout_path, method: :delete %> |
        <% else %>
          <%= link_to 'login', login_path %> |
          <%= link_to 'create an account', signup_path %> |
        <% end %>
        <%= link_to 'home', root_path %> |

          <div class="dropdown" >
              <button class="btn btn-sm dropdown-toggle" type="button" id="droppeddownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                  recipes
              </button>
              <ul class="dropdown-menu" aria-labelledby="droppeddownMenu1">
                <li><%= link_to "top 10", ratings_path %> </li>
                <li class="divider"></li>
                <li><%= link_to "categories", recipes_path %> </li>
              </ul>
        </div> 
    </nav>
          

  
</header>


